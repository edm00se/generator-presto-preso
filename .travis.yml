sudo: false
language: node_js
notifications:
  email: false
cache:
  yarn: true
node_js:
  - node
  - lts/*
before_install:
  - yarn global add codecov
  - npm install -g npm@latest
script:
  - yarn test:ci
after_success:
  - yarn semantic-release
  - codecov
